# Auth — passcode for login gate
DEMO_PASSWORD=masala2026

# Claude API (server-side only — set in Vercel env)
ANTHROPIC_API_KEY=

# ElevenLabs (server-side only)
ELEVENLABS_API_KEY=
ELEVENLABS_AGENT_ID=

# NLM reference (CLI / agent context)
NOTEBOOKLM_NOTEBOOK_ID=b5db93de-0a0e-49ea-9cd9-6009beabf6ed

# Optional: enable live Claude ChatAgent (default: offline knowledge router)
# VITE_ENABLE_LIVE_CHAT=false
